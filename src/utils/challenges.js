import { JSChallenge } from "./classes";

const data = [
  new JSChallenge(
    "Write a function called 'sum' that takes in two values, and returns the sum.",
    { params: [22, 22] },
    ["Math", "Functions"]
  ),
];

export default data;
